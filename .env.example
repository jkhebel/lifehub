# Optional: Supabase for Phase 3 auth and per-user persistence.
# Leave unset for local-only mode (no sign-in UI).
# Get URL and anon key from: Supabase project → Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
